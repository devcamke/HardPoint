<div id="pos_message" aria-live="polite">
  <% if alert %>
    <p class="rounded-md bg-red-50 px-4 py-2 text-sm font-medium text-red-800"><%= alert %></p>
  <% elsif message %>
    <p class="rounded-md bg-green-50 px-4 py-2 text-sm font-medium text-green-800"><%= message %></p>
  <% end %>
</div>
