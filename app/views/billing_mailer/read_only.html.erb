<p style="margin-top:0;">Hello,</p>
<p>HardPoint invoice <strong><%= @invoice.number %></strong> (<%= money(@invoice.amount_cents, currency: @invoice.currency) %>) was due on
  <%= @invoice.due_on.to_fs(:long) %>, so <strong><%= @account.name %></strong> is now read-only. Nothing is lost: you can see and export everything.
  Selling and changes come back the moment it's paid.</p>
<%= render "billing_mailer/pay" %>
