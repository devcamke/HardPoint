<!DOCTYPE html>
<html lang="en">
  <head><title><%= Current.account.name %></title><meta name="viewport" content="width=device-width,initial-scale=1"><%= stylesheet_link_tag :app %></head>
  <body class="flex min-h-screen items-center justify-center bg-concrete-50 px-4 text-navy-900">
    <div class="text-center">
      <h1 class="text-2xl font-bold"><%= Current.account.name %></h1>
      <p class="mt-2 text-steel-600">This shop doesn't have an online store.</p>
    </div>
  </body>
</html>
