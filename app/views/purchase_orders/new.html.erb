<% content_for :title, "New purchase order" %>
<p class="text-sm"><%= link_to "← Purchase orders", purchase_orders_path, class: "link" %></p>
<h1 class="mt-2 mb-6 text-3xl font-bold">New purchase order</h1>
<%= render "form", purchase_order: @purchase_order %>
