<!DOCTYPE html>
<html>
  <head>
    <title>Customer display · <%= Current.account.name %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag :app %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="h-screen overflow-hidden bg-navy-900 text-white">
    <%= yield %>
  </body>
</html>
