<% content_for :title, "Add webhook endpoint" %>
<p class="text-sm"><%= link_to "← Developers", developers_path, class: "link" %></p>
<h1 class="mt-2 mb-6 text-3xl font-bold">Add webhook endpoint</h1>
<%= render "form", endpoint: @endpoint %>
